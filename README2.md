## 1.var: var is the oldest way to declare variable .It has functional scope and it can create problem is big problem.so developer generally don't use it now.
## let: let is introduced in ES6 . It has block scope. Developer is let insted of var now. 
## const : const means constant . This variable is also introduced in ES6.It has block element. the value of const can not be re-assigned.


## 2. spread operator is introduced in ES6. It is used to edit array , object .

## 3. map(), filter() and forEach are used in arrays but they do different work in the array. map() is used to copy the original array. filter() create a new array with the value which setisty the condition. At last , forEach is used to show the element of an array differently.

## 4. arrow function is the shortest away to declare a function . It make code more clean and it is introduced in ES6.

## 5. What are template literals is used by developer to write string in more flexible way . It use `` instead of "" or ''. It is also introduced in ES6.
